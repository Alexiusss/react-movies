import { MovieList } from "../components/MovieList";
function Main() {
    return (
        <main className="container content">
            Hello from Movies App
            <MovieList />
        </main>
    );
}

export { Main };
